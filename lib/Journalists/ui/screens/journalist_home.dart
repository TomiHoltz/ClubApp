import 'package:flutter/material.dart';

class JournalistHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Success"),
    );
  }
}